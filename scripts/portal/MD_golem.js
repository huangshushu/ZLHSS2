
function enter(pi) {
	pi.openNpc(2003,2);
        /*if (pi.getMapId() == baseid) {
            for(var i = 0; i < dungeons; i++) {
                if (pi.getPlayerCount(dungeonid + i) == 0) {
                    pi.warp(dungeonid + i, 0);
                    return true;
                }
            }
            pi.playerMessage(1, "此路不通，禁止出入。");
			pi.dispose();
        } else {
        pi.warp(baseid, "MD00");
        }
        return true;*/
}